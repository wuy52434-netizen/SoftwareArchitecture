# 前端 UI 自动化（Playwright + POM）
[pytest]
testpaths = .
python_files = test_*.py
# --base-url 由 conftest 的 browser_context_args 注入，页面对象可直接用相对路径
addopts = -v --tb=short -p no:cacheprovider
# HTML 报告由运行脚本显式传入 --html
